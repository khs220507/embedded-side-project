# 저장소 작업 규칙

## 설명 원칙

- 코드 설명은 현재 학습 로드맵의 단계와 사용자가 직접 구현한 코드를 중심으로 한다.
- 사용자가 코드의 핵심을 요청하면 실제 동작에 직접 관련된 부분만 우선 설명한다.
- 이미 설명하거나 학습한 개념은 새로운 차이점이 없으면 반복해서 설명하지 않는다.
- 핀, LED 색상, 변수 이름만 달라지는 동일한 구현은 별도 학습 단계로 길게 다루지 않는다.
- STM32CubeMX가 생성한 초기화 코드, 클록 설정, 주변 코드 등은 현재 학습 주제에 필요할 때만 설명한다.
- 아직 구현하지 않은 기능을 현재 구현 내용처럼 기록하지 않는다.
- 설명은 한국어로 작성하고, 함수명·핀 이름·프로토콜명 등의 기술 용어는 원문 표기를 유지한다.

## 블로그 기록 원칙

- 블로그 글은 프로젝트별이 아니라 기술별로 분류한다.
- 모든 블로그 기록은 저장소 루트의 `blog/` 디렉터리에 저장한다.
- 파일은 기술 이름을 사용한다. 예: `GPIO.md`, `UART.md`, `Interrupt.md`, `Timer.md`.
- GPIO 학습 내용은 프로젝트 번호와 관계없이 `blog/GPIO.md`에 기록한다.
- UART 학습 내용은 프로젝트 번호와 관계없이 `blog/UART.md`에 기록한다.
- 하나의 구현에 여러 기술이 포함되면 각 기술의 핵심 내용을 해당 기술 문서에 나누어 기록한다.
- 같은 기술의 새로운 학습 내용은 기존 기술 문서에 새로운 절로 추가한다.
- 기존 절과 핵심 개념이 동일하면 새 절을 만들지 않고, 필요한 차이점이나 확장 내용만 추가한다.
- 블로그 문서 안에서 같은 코드 설명과 결론을 반복하지 않는다.
- 사용자가 블로그에 기록해 달라고 하면 현재 실제로 구현하고 확인한 내용만 기록한다.

## 학습 챕터와 프로젝트 폴더

- 새로운 기술 학습 챕터로 넘어가면 저장소 루트에 다음 순번의 전용 프로젝트 폴더를 자동으로 생성한다.
- 프로젝트 폴더 이름은 `번호_기술명` 형식을 사용한다. 예: `01_GPIO_LED`, `02_UART`, `03_Interrupt`.
- 새 챕터의 코드는 이전 챕터 폴더에 계속 추가하지 않고 새 프로젝트 폴더에서 구현한다.
- 이전 챕터 프로젝트는 해당 학습 완료 상태로 보존하며, 새 챕터 생성 과정에서 이전 코드를 손실시키지 않는다.
- 이전 프로젝트를 기반으로 복제할 때는 빌드 산출물을 제외하고 소스와 설정을 복사한 뒤 프로젝트명, `.ioc`, CMake, IDE 설정의 내부 이름을 새 폴더명과 일치시킨다.
- 새 프로젝트를 만든 뒤 이전 프로젝트와 새 프로젝트를 각각 빌드해 독립적으로 동작 가능한 구조인지 확인한다.
- 새 챕터의 학습 내용은 동시에 해당 기술 문서(`blog/기술명.md`, 필요한 경우 `.html`)에 기록한다.
- 실제 보드 동작을 확인하지 않은 기능은 구현 또는 빌드 상태와 하드웨어 검증 상태를 구분해서 기록한다.

## 블로그 글 구성

기술 문서의 각 학습 기록은 가능한 한 다음 순서로 작성한다.

1. 구현 목표
2. 핵심 코드
3. 코드 설명
4. 동작 흐름 또는 실행 결과
5. 배운 점
6. 한 줄 정리

필요한 경우 배선, 문제와 해결 과정, 주의 사항을 추가할 수 있다. 현재 주제와 관계없는 자동 생성 코드에 대한 장황한 설명은 넣지 않는다.

## 기술 문서 HTML 스타일

- 기술 문서의 HTML 버전은 `blog/GPIO.html`의 CSS와 문서 구조를 기준으로 통일한다.
- 기술별로 `기술명.md`와 `기술명.html`을 함께 관리한다. 예: `GPIO.md`, `GPIO.html`.
- HTML의 `<title>`과 `<h1>`은 `STM32 {기술명}: {핵심 실습}` 형식을 사용한다. 예: `STM32 GPIO: LED와 버튼 제어`.
- 상단 영문 라벨은 `Embedded Study Note`로 통일하고, 제목 아래에는 문서 범위를 설명하는 한 문장만 작성한다.
- 흰색 배경, 최대 본문 너비 820px, 파란색 단일 포인트 색상, 얇은 회색 구분선을 사용한다.
- 그라데이션, 과도한 그림자, 장식적인 카드 UI는 사용하지 않는다.
- 상단에는 기술명, 간단한 설명, 문서 내부 목차를 배치한다.
- 코드 블록은 밝은 회색 배경을 사용하고 가로 스크롤이 가능해야 한다.
- 개념도와 동작 흐름은 공백으로 정렬한 ASCII 표현 대신 박스와 단계형 구조를 사용한다.
- 표는 `width: 100%`와 `table-layout: fixed`를 사용해 본문 너비를 채우고, 긴 기술 용어가 셀 밖으로 잘리지 않게 한다.
- 모바일 화면에서도 본문, 표, 개념도, 동작 흐름이 잘리거나 레이아웃 밖으로 넘치지 않도록 반응형 스타일을 적용한다.
- HTML 문서에는 같은 기술의 Markdown 문서에 기록된 기존 내용과 새로 추가한 내용을 모두 포함한다.
- Markdown 내용이 변경되면 같은 기술의 HTML 내용도 함께 갱신한다.
- 문서 내용은 `기본 개념 → 구현 목표 → 핵심 코드 → 코드 설명 → 동작 흐름 → 배운 점 → 한 줄 정리` 순서를 기본으로 한다.

## 코드와 문서의 일치

- 블로그 기록을 작성하기 전에 실제 소스 코드를 확인한다.
- GPIO 포트, 핀 번호, 지연 시간, 통신 속도 등의 값은 코드와 일치해야 한다.
- 코드가 변경되어 기존 블로그 설명과 달라지면 관련 기술 문서도 함께 갱신한다.
- 코드의 동작을 검증하지 못했다면 검증된 결과처럼 표현하지 않는다.
